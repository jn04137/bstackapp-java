spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/bstack_db
    username: bstack_user
    password: password
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
